#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * mul - a function that multiplies two integers
 * @a: the integer that we want to multibly
 * @b: the integer that we want to multibly
 * Return: the result of multiblication
 */
int mul(int a, int b)
{
int c;

c = a * b;
return (c);
}
