#include "main.h"
#include <stdio.h>
/**
 * add - a function that adds two integers
 * Return:returns the result
 * @a: the number we want to add
 * @b: the number we want to add
 */
int add(int a, int b)
{
int c;

c = a + b;
return (c);
}
